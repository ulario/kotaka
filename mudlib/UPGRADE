To upgrade to version 0.38 from version 0.37:

First, make sure that callouts are not currently suspended
(this version destructs and reloads the callout manager)

Then use the upgrade command, followed by rebuild.
